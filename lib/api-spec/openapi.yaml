openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.4.0
  description: MOB HUB Egypt — Mobile Phone Marketplace API
servers:
  - url: /api
    description: Base API path

tags:
  - name: auth
    description: Authentication and session management
  - name: health
    description: Health operations
  - name: stores
    description: Store operations
  - name: products
    description: Product operations
  - name: phoneSpecs
    description: Phone specification operations
  - name: categories
    description: Category operations
  - name: dashboard
    description: Seller dashboard operations
  - name: messaging
    description: Buyer–Seller conversation and messaging
  - name: notifications
    description: Centralized notification platform (buyer + seller)
  - name: reservations
    description: Product reservation workflow (buyer browses → reserves → visits physical store)

paths:
  # ─── Auth ────────────────────────────────────────────────────────────────────

  /auth/register:
    post:
      operationId: authRegister
      tags: [auth]
      summary: Register a new user account
      description: >
        Creates a new buyer or merchant account, returns JWT access + refresh tokens
        and a public user object. The access token expires in 15 minutes; the
        refresh token is long-lived and must be kept in secure storage.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterBody"
      responses:
        "201":
          description: Account created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthTokensResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: Email already registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/login:
    post:
      operationId: authLogin
      tags: [auth]
      summary: Log in with email and password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginBody"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthTokensResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/refresh:
    post:
      operationId: authRefresh
      tags: [auth]
      summary: Rotate refresh token and issue a new access token
      description: >
        Accepts the current refresh token and returns a brand-new access + refresh
        token pair. The old refresh token is immediately revoked. Presenting a
        previously-revoked token triggers full session termination (reuse detection).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshBody"
      responses:
        "200":
          description: Tokens rotated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthTokensResponse"
        "401":
          description: Invalid or expired refresh token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/logout:
    post:
      operationId: authLogout
      tags: [auth]
      summary: Revoke the current session
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogoutBody"
      responses:
        "204":
          description: Session revoked
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/logout-all:
    post:
      operationId: authLogoutAll
      tags: [auth]
      summary: Revoke all sessions for the authenticated user
      security:
        - BearerAuth: []
      responses:
        "204":
          description: All sessions revoked
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/me:
    get:
      operationId: authMe
      tags: [auth]
      summary: Return the authenticated user's profile
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserDto"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/forgot-password:
    post:
      operationId: authForgotPassword
      tags: [auth]
      summary: Request a password-reset token
      description: >
        Sends a reset link/token to the email address if it exists. In non-production
        the response includes `_devToken` for testing without an email provider.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgotPasswordBody"
      responses:
        "200":
          description: Reset token sent (or queued)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /auth/reset-password:
    post:
      operationId: authResetPassword
      tags: [auth]
      summary: Set a new password using a reset token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordBody"
      responses:
        "200":
          description: Password updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: Invalid or expired reset token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/send-verification:
    post:
      operationId: authSendVerification
      tags: [auth]
      summary: Re-send the email-verification token
      security:
        - BearerAuth: []
      description: >
        In non-production the response includes `_devToken` for testing without
        an email provider.
      responses:
        "200":
          description: Verification token sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/verify-email:
    post:
      operationId: authVerifyEmail
      tags: [auth]
      summary: Verify an email address using the one-time token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyEmailBody"
      responses:
        "200":
          description: Email verified
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: Invalid or expired verification token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /stores:
    get:
      operationId: getStores
      tags: [stores]
      summary: List stores
      parameters:
        - name: search
          in: query
          schema: { type: string }
        - name: governorate
          in: query
          schema: { type: string }
        - name: isVerified
          in: query
          schema: { type: string }
        - name: ids
          in: query
          description: Comma-separated store IDs
          schema: { type: string }
        - name: sort
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Array of stores
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StoreDto"

  /stores/{id}:
    get:
      operationId: getStore
      tags: [stores]
      summary: Get store by ID
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoreDto"
        "404":
          description: Not found

  /stores/{id}/products:
    get:
      operationId: getStoreProducts
      tags: [stores]
      summary: Get products for a store
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProductDto"

  /stores/{id}/reviews:
    get:
      operationId: getStoreReviews
      tags: [stores]
      summary: Get reviews for a store
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReviewDto"
    post:
      operationId: createStoreReview
      tags: [stores]
      summary: Submit a review for a store
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReviewBody"
      responses:
        "201":
          description: Created review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewDto"
        "400":
          description: Validation error
        "404":
          description: Store not found
        "409":
          description: Duplicate review

  /reviews/{id}:
    put:
      operationId: updateReview
      tags: [stores]
      summary: Update an existing review
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReviewBody"
      responses:
        "200":
          description: Updated review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewDto"
        "400":
          description: Validation error
        "403":
          description: Not your review
        "404":
          description: Review not found
    delete:
      operationId: deleteReview
      tags: [stores]
      summary: Delete a review
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: query
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Deleted
        "403":
          description: Not your review
        "404":
          description: Review not found

  /products:
    get:
      operationId: getProducts
      tags: [products]
      summary: List / filter products
      parameters:
        - name: storeId
          in: query
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
        - name: isNew
          in: query
          schema: { type: string }
        - name: isBestSeller
          in: query
          schema: { type: string }
        - name: isFeatured
          in: query
          schema: { type: string }
        - name: condition
          in: query
          schema: { type: string }
        - name: ids
          in: query
          description: Comma-separated product IDs
          schema: { type: string }
        - name: excludeId
          in: query
          schema: { type: string }
        - name: search
          in: query
          schema: { type: string }
        - name: brand
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProductDto"

  /products/{id}:
    get:
      operationId: getProduct
      tags: [products]
      summary: Get product by ID
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductDto"
        "404":
          description: Not found

  /phone-specs:
    get:
      operationId: getPhoneSpecs
      tags: [phoneSpecs]
      summary: List all phone specs
      responses:
        "200":
          description: Phone specs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PhoneSpecDto"

  /phone-specs/{id}:
    get:
      operationId: getPhoneSpec
      tags: [phoneSpecs]
      summary: Get phone spec by ID
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Phone spec
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhoneSpecDto"
        "404":
          description: Not found

  /categories:
    get:
      operationId: getCategories
      tags: [categories]
      summary: List all categories
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CategoryDto"

  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [dashboard]
      summary: Get dashboard stats for a store
      parameters:
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStatsDto"

  /dashboard/orders:
    get:
      operationId: getDashboardOrders
      tags: [dashboard]
      summary: List orders for a store
      parameters:
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OrderDto"

  /dashboard/messages:
    get:
      operationId: getDashboardMessages
      tags: [dashboard]
      summary: List messages for a store
      parameters:
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MessageDto"

  /dashboard/reviews:
    get:
      operationId: getDashboardReviews
      tags: [dashboard]
      summary: List reviews for a store (dashboard)
      parameters:
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReviewDto"

  /dashboard/products:
    post:
      operationId: createDashboardProduct
      tags: [dashboard]
      summary: Create a new product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProductBody"
      responses:
        "201":
          description: Created product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductDto"
        "400":
          description: Validation error

  # ───────────────────────────────────────────────────────────────────────────
  # Messaging
  # ───────────────────────────────────────────────────────────────────────────

  /conversations:
    get:
      operationId: getConversations
      tags: [messaging]
      summary: List conversations (by buyerId or storeId)
      parameters:
        - name: buyerId
          in: query
          schema: { type: string }
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: List of conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConversationDto"
        "400":
          description: At least one of buyerId or storeId is required
    post:
      operationId: createConversation
      tags: [messaging]
      summary: Create or retrieve an existing conversation (idempotent)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateConversationBody"
      responses:
        "200":
          description: Existing conversation returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationDto"
        "201":
          description: New conversation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationDto"
        "400":
          description: Validation error
        "404":
          description: Store not found

  /conversations/{id}:
    get:
      operationId: getConversation
      tags: [messaging]
      summary: Get a conversation by ID (participant check required)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: buyerId
          in: query
          schema: { type: string }
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationDto"
        "403":
          description: Not a participant
        "404":
          description: Not found

  /conversations/{id}/messages:
    get:
      operationId: getConversationMessages
      tags: [messaging]
      summary: List messages in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: buyerId
          in: query
          schema: { type: string }
        - name: storeId
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatMessageDto"
        "403":
          description: Not a participant
        "404":
          description: Conversation not found
    post:
      operationId: sendMessage
      tags: [messaging]
      summary: Send a message in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageBody"
      responses:
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatMessageDto"
        "400":
          description: Validation error
        "403":
          description: Not a participant
        "404":
          description: Conversation not found

  # ───────────────────────────────────────────────────────────────────────────
  # Notifications
  # ───────────────────────────────────────────────────────────────────────────

  /notifications:
    get:
      operationId: getNotifications
      tags: [notifications]
      summary: List notifications for a recipient (buyer device UUID or seller storeId)
      parameters:
        - name: userId
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Notifications, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NotificationDto"
        "400":
          description: userId is required

  /notifications/unread-count:
    get:
      operationId: getNotificationsUnreadCount
      tags: [notifications]
      summary: Get the unread notification count for a recipient
      parameters:
        - name: userId
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Unread count
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnreadCountDto"
        "400":
          description: userId is required

  /notifications/read-all:
    patch:
      operationId: markAllNotificationsRead
      tags: [notifications]
      summary: Mark every notification read for a recipient
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkAllNotificationsReadBody"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                required: [success]
        "400":
          description: userId is required

  /notifications/{id}:
    get:
      operationId: getNotification
      tags: [notifications]
      summary: Get a single notification by ID
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: query
          required: true
          description: Recipient ID — must match the notification owner
          schema: { type: string }
      responses:
        "200":
          description: Notification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationDto"
        "403":
          description: Forbidden — userId does not match notification owner
        "404":
          description: Notification not found
    delete:
      operationId: deleteNotification
      tags: [notifications]
      summary: Delete a notification
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: query
          required: true
          description: Recipient ID — must match the notification owner
          schema: { type: string }
      responses:
        "204":
          description: Deleted
        "403":
          description: Forbidden — userId does not match notification owner
        "404":
          description: Notification not found

  /notifications/{id}/read:
    patch:
      operationId: markNotificationRead
      tags: [notifications]
      summary: Mark a single notification read
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: query
          required: true
          description: Recipient ID — must match the notification owner
          schema: { type: string }
      responses:
        "200":
          description: Updated notification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationDto"
        "403":
          description: Forbidden — userId does not match notification owner
        "404":
          description: Notification not found

  # ───────────────────────────────────────────────────────────────────────────
  # Reservations
  # ───────────────────────────────────────────────────────────────────────────

  /products/{id}/reserve:
    post:
      operationId: createReservation
      tags: [reservations]
      summary: Create a reservation for a product
      description: >
        A buyer submits a reservation request for a specific product.
        The product must exist. Only one active (pending or confirmed) reservation
        is allowed per product at any time — the DB enforces this with a unique
        partial index. A conversation thread is automatically created or reused.
        Sends a `reservation_created` notification to the merchant.
      parameters:
        - name: id
          in: path
          required: true
          description: Product ID to reserve
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReservationBody"
            example:
              buyerId: "device_abc123"
              buyerNotes: "هل يمكن تجربة الجهاز قبل الشراء؟"
      responses:
        "201":
          description: Reservation created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationDto"
        "400":
          description: Validation error — buyerId is required
        "404":
          description: Product not found
        "409":
          description: Conflict — product already has an active reservation, or buyer has a duplicate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationConflictError"

  /reservations:
    get:
      operationId: getReservations
      tags: [reservations]
      summary: List reservations for a buyer or merchant
      description: >
        Returns reservations filtered by buyerId (buyer's device UUID) or storeId
        (merchant). At least one must be provided. Supports optional status filter
        and pagination via limit/offset.
      parameters:
        - name: buyerId
          in: query
          description: Buyer device UUID — returns all reservations made by this buyer
          schema: { type: string }
        - name: storeId
          in: query
          description: Store ID — returns all reservations for this store's products
          schema: { type: string }
        - name: status
          in: query
          description: Filter by reservation status (pending, confirmed, declined, cancelled, expired, completed)
          schema: { type: string }
        - name: limit
          in: query
          description: Maximum number of results (1–50, default 20)
          schema: { type: integer }
        - name: offset
          in: query
          description: Pagination offset (default 0)
          schema: { type: integer }
      responses:
        "200":
          description: List of reservations ordered by createdAt descending
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReservationDto"
        "400":
          description: At least one of buyerId or storeId is required

  /reservations/{id}:
    get:
      operationId: getReservation
      tags: [reservations]
      summary: Get a single reservation by ID
      description: >
        Fetches a reservation by its ID. Either buyerId or storeId must be provided
        for access control — the API returns 404 (not 403) when the caller is not
        a participant to prevent ID enumeration.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: buyerId
          in: query
          description: Buyer device UUID (required if caller is the buyer)
          schema: { type: string }
        - name: storeId
          in: query
          description: Store ID (required if caller is the merchant)
          schema: { type: string }
      responses:
        "200":
          description: Reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationDto"
        "404":
          description: Reservation not found or caller is not a participant

  /reservations/{id}/confirm:
    patch:
      operationId: confirmReservation
      tags: [reservations]
      summary: Merchant confirms a pending reservation
      description: >
        Transitions the reservation from `pending` → `confirmed`.
        Only the owning store may confirm. Posts a system message to the linked
        conversation and sends a `reservation_confirmed` notification to the buyer.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MerchantActionBody"
            example:
              storeId: "s1"
      responses:
        "200":
          description: Updated reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationDto"
        "400":
          description: storeId is required
        "403":
          description: Forbidden — caller is not the owning store
        "404":
          description: Reservation not found
        "422":
          description: Invalid state transition — reservation is not in pending status

  /reservations/{id}/decline:
    patch:
      operationId: declineReservation
      tags: [reservations]
      summary: Merchant declines a pending reservation
      description: >
        Transitions the reservation from `pending` → `declined`.
        Only the owning store may decline. Sends a `reservation_declined`
        notification to the buyer.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeclineReservationBody"
            example:
              storeId: "s1"
              cancellationReason: "الجهاز غير متاح حالياً"
      responses:
        "200":
          description: Updated reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationDto"
        "400":
          description: storeId is required
        "403":
          description: Forbidden — caller is not the owning store
        "404":
          description: Reservation not found
        "422":
          description: Invalid state transition

  /reservations/{id}/cancel:
    patch:
      operationId: cancelReservation
      tags: [reservations]
      summary: Cancel a reservation (buyer or merchant)
      description: >
        Transitions the reservation from `pending` or `confirmed` → `cancelled`.
        Either the buyer (buyerId) or the merchant (storeId) may cancel.
        Provide exactly one of buyerId or storeId. The other party receives a
        `reservation_cancelled` notification.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelReservationBody"
            example:
              buyerId: "device_abc123"
              cancellationReason: "غيّرت رأيي"
      responses:
        "200":
          description: Updated reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationDto"
        "400":
          description: Either buyerId or storeId is required
        "404":
          description: Reservation not found or caller is not a participant
        "422":
          description: Invalid state transition — terminal statuses cannot be cancelled

  /reservations/{id}/complete:
    patch:
      operationId: completeReservation
      tags: [reservations]
      summary: Merchant marks a confirmed reservation as completed
      description: >
        Transitions the reservation from `confirmed` → `completed`.
        Only the owning store may complete. Represents the buyer visiting
        the physical store. Sends a `reservation_completed` notification to the buyer.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MerchantActionBody"
            example:
              storeId: "s1"
      responses:
        "200":
          description: Updated reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationDto"
        "400":
          description: storeId is required
        "403":
          description: Forbidden — caller is not the owning store
        "404":
          description: Reservation not found
        "422":
          description: Invalid state transition — reservation is not in confirmed status

  /conversations/{id}/read:
    patch:
      operationId: markConversationRead
      tags: [messaging]
      summary: Mark a conversation as read for a participant
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkReadBody"
      responses:
        "200":
          description: Updated conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationDto"
        "400":
          description: Invalid readerType
        "404":
          description: Conversation not found

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        JWT access token issued by POST /auth/login or POST /auth/register.
        Pass as `Authorization: Bearer <token>`.

  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    StoreDto:
      type: object
      properties:
        id: { type: string }
        nameAr: { type: string }
        nameEn: { type: string }
        descriptionAr: { type: string }
        descriptionEn: { type: string }
        logoColor: { type: string }
        logoInitial: { type: string }
        coverGradient:
          type: array
          items: { type: string }
        rating: { type: number }
        reviewsCount: { type: integer }
        governorate: { type: string }
        city: { type: string }
        address: { type: string }
        addressAr: { type: string }
        lat: { type: number }
        lng: { type: number }
        phone: { type: string }
        whatsapp: { type: string }
        facebook:
          type: string
          nullable: true
        instagram:
          type: string
          nullable: true
        website:
          type: string
          nullable: true
        workingHours: { type: string }
        workingHoursAr: { type: string }
        isOpen: { type: boolean }
        isVerified: { type: boolean }
        productsCount: { type: integer }
        categories:
          type: array
          items: { type: string }
      required:
        - id
        - nameAr
        - nameEn
        - descriptionAr
        - descriptionEn
        - logoColor
        - logoInitial
        - coverGradient
        - rating
        - reviewsCount
        - governorate
        - city
        - address
        - addressAr
        - lat
        - lng
        - phone
        - whatsapp
        - workingHours
        - workingHoursAr
        - isOpen
        - isVerified
        - productsCount
        - categories

    ProductDto:
      type: object
      properties:
        id: { type: string }
        storeId: { type: string }
        nameAr: { type: string }
        nameEn: { type: string }
        descriptionAr: { type: string }
        descriptionEn: { type: string }
        brand: { type: string }
        model: { type: string }
        price: { type: integer }
        discountPrice:
          type: integer
          nullable: true
        category: { type: string }
        condition: { type: string }
        inStock: { type: boolean }
        warranty: { type: string }
        warrantyAr: { type: string }
        colors:
          type: array
          items: { type: string }
        storage:
          type: array
          items: { type: string }
          nullable: true
        ram:
          type: array
          items: { type: string }
          nullable: true
        imageColor: { type: string }
        rating: { type: number }
        reviewsCount: { type: integer }
        isNew: { type: boolean }
        isBestSeller: { type: boolean }
        isFeatured: { type: boolean }
      required:
        - id
        - storeId
        - nameAr
        - nameEn
        - descriptionAr
        - descriptionEn
        - brand
        - model
        - price
        - category
        - condition
        - inStock
        - warranty
        - warrantyAr
        - colors
        - imageColor
        - rating
        - reviewsCount
        - isNew
        - isBestSeller
        - isFeatured

    PhoneSpecDto:
      type: object
      properties:
        id: { type: string }
        brand: { type: string }
        model: { type: string }
        os: { type: string }
        processor: { type: string }
        ram:
          type: array
          items: { type: string }
        storage:
          type: array
          items: { type: string }
        displaySize: { type: string }
        displayType: { type: string }
        resolution: { type: string }
        refreshRate: { type: string }
        brightness: { type: string }
        rearCamera: { type: string }
        frontCamera: { type: string }
        videoRecording: { type: string }
        battery: { type: string }
        charging: { type: string }
        wirelessCharging: { type: boolean }
        reverseCharging: { type: boolean }
        fingerprint: { type: boolean }
        faceUnlock: { type: boolean }
        waterResistance: { type: string }
        weight: { type: string }
        dimensions: { type: string }
        fiveG: { type: boolean }
        nfc: { type: boolean }
        bluetooth: { type: string }
        wifi: { type: string }
        usb: { type: string }
        audioJack: { type: boolean }
        colors:
          type: array
          items: { type: string }
        priceEGP: { type: integer }
        releaseDate: { type: string }
        _batteryMah: { type: integer }
        _chargingW: { type: integer }
        _refreshRateHz: { type: integer }
        _rearMp: { type: integer }
        _ramGb: { type: integer }
        _displayInch: { type: number }
      required:
        - id
        - brand
        - model
        - os
        - processor
        - ram
        - storage
        - displaySize
        - displayType
        - resolution
        - refreshRate
        - brightness
        - rearCamera
        - frontCamera
        - videoRecording
        - battery
        - charging
        - wirelessCharging
        - reverseCharging
        - fingerprint
        - faceUnlock
        - waterResistance
        - weight
        - dimensions
        - fiveG
        - nfc
        - bluetooth
        - wifi
        - usb
        - audioJack
        - colors
        - priceEGP
        - releaseDate
        - _batteryMah
        - _chargingW
        - _refreshRateHz
        - _rearMp
        - _ramGb
        - _displayInch

    CategoryDto:
      type: object
      properties:
        id: { type: string }
        icon: { type: string }
        color: { type: string }
      required: [id, icon, color]

    ReviewDto:
      type: object
      properties:
        id: { type: string }
        storeId:
          type: string
          nullable: true
        productId:
          type: string
          nullable: true
        userId:
          type: string
          nullable: true
        author: { type: string }
        authorAr: { type: string }
        rating: { type: integer }
        title: { type: string }
        textAr: { type: string }
        textEn: { type: string }
        date: { type: string }
        status: { type: string }
        helpfulCount: { type: integer }
        verifiedPurchase: { type: boolean }
        createdAt: { type: string }
        updatedAt: { type: string }
      required:
        - id
        - author
        - authorAr
        - rating
        - title
        - textAr
        - textEn
        - date
        - status
        - helpfulCount
        - verifiedPurchase
        - createdAt
        - updatedAt

    CreateReviewBody:
      type: object
      properties:
        author: { type: string }
        authorAr: { type: string }
        rating: { type: integer }
        title: { type: string }
        textEn: { type: string }
        textAr: { type: string }
        userId: { type: string }
      required:
        - author
        - authorAr
        - rating
        - textEn
        - textAr

    UpdateReviewBody:
      type: object
      properties:
        rating: { type: integer }
        title: { type: string }
        textEn: { type: string }
        textAr: { type: string }
        userId: { type: string }
      required:
        - rating
        - textEn
        - textAr
        - userId

    DashboardStatsDto:
      type: object
      properties:
        storeId: { type: string }
        views: { type: integer }
        visitors: { type: integer }
        clicks: { type: integer }
        reservations: { type: integer }
        newReviews: { type: integer }
        messagesCount: { type: integer }
        saved: { type: integer }
        trending: { type: integer }
      required:
        - storeId
        - views
        - visitors
        - clicks
        - reservations
        - newReviews
        - messagesCount
        - saved
        - trending

    OrderDto:
      type: object
      properties:
        id: { type: string }
        productAr: { type: string }
        productEn: { type: string }
        storage:
          type: string
          nullable: true
        customer: { type: string }
        timeAr: { type: string }
        timeEn: { type: string }
        status: { type: string }
        price: { type: integer }
      required:
        - id
        - productAr
        - productEn
        - customer
        - timeAr
        - timeEn
        - status
        - price

    MessageDto:
      type: object
      properties:
        id: { type: string }
        customer: { type: string }
        productAr: { type: string }
        productEn: { type: string }
        timeAr: { type: string }
        timeEn: { type: string }
        status: { type: string }
        unread: { type: integer }
      required:
        - id
        - customer
        - productAr
        - productEn
        - timeAr
        - timeEn
        - status
        - unread

    CreateProductBody:
      type: object
      properties:
        storeId: { type: string }
        nameAr: { type: string }
        nameEn: { type: string }
        descriptionAr: { type: string }
        descriptionEn: { type: string }
        brand: { type: string }
        model: { type: string }
        price: { type: integer }
        discountPrice:
          type: integer
          nullable: true
        category: { type: string }
        condition: { type: string }
        warranty: { type: string }
        warrantyAr: { type: string }
        colors:
          type: array
          items: { type: string }
        storage:
          type: array
          items: { type: string }
          nullable: true
        ram:
          type: array
          items: { type: string }
          nullable: true
        imageColor: { type: string }
        inStock: { type: boolean }
      required:
        - nameAr
        - nameEn
        - brand
        - model
        - price
        - category

    # ─── Messaging schemas ────────────────────────────────────────────────────

    ConversationDto:
      type: object
      properties:
        id: { type: string }
        storeId: { type: string }
        buyerId: { type: string }
        productId:
          type: string
          nullable: true
        productNameAr:
          type: string
          nullable: true
        productNameEn:
          type: string
          nullable: true
        lastMessageText:
          type: string
          nullable: true
        lastMessageAt:
          type: string
          nullable: true
        buyerUnreadCount: { type: integer }
        sellerUnreadCount: { type: integer }
        status: { type: string }
        createdAt: { type: string }
        updatedAt: { type: string }
      required:
        - id
        - storeId
        - buyerId
        - buyerUnreadCount
        - sellerUnreadCount
        - status
        - createdAt
        - updatedAt

    ChatMessageDto:
      type: object
      properties:
        id: { type: string }
        conversationId: { type: string }
        senderType: { type: string }
        senderId: { type: string }
        type: { type: string }
        content: { type: string }
        status: { type: string }
        createdAt: { type: string }
      required:
        - id
        - conversationId
        - senderType
        - senderId
        - type
        - content
        - status
        - createdAt

    CreateConversationBody:
      type: object
      properties:
        buyerId: { type: string }
        storeId: { type: string }
        productId: { type: string }
        productNameAr: { type: string }
        productNameEn: { type: string }
      required:
        - buyerId
        - storeId

    SendMessageBody:
      type: object
      properties:
        senderId: { type: string }
        senderType: { type: string }
        content: { type: string }
        type: { type: string }
      required:
        - senderId
        - senderType
        - content

    MarkReadBody:
      type: object
      properties:
        readerType: { type: string }
      required:
        - readerType

    # ─── Notification schemas ─────────────────────────────────────────────────

    NotificationDto:
      type: object
      properties:
        id: { type: string }
        userId: { type: string }
        type: { type: string }
        titleAr: { type: string }
        titleEn: { type: string }
        bodyAr: { type: string }
        bodyEn: { type: string }
        metadata:
          type: object
          nullable: true
          additionalProperties: true
        readStatus: { type: boolean }
        createdAt: { type: string }
        expiresAt:
          type: string
          nullable: true
      required:
        - id
        - userId
        - type
        - titleAr
        - titleEn
        - bodyAr
        - bodyEn
        - readStatus
        - createdAt

    UnreadCountDto:
      type: object
      properties:
        unreadCount: { type: integer }
      required:
        - unreadCount

    MarkAllNotificationsReadBody:
      type: object
      properties:
        userId: { type: string }
      required:
        - userId

    # ─── Reservation schemas ──────────────────────────────────────────────────

    ReservationProductSnapshot:
      type: object
      description: Embedded product summary within a ReservationDto — avoids a second round-trip
      properties:
        nameAr: { type: string }
        nameEn: { type: string }
        price: { type: integer }
        imageColor: { type: string }
        brand: { type: string }
        model: { type: string }
      required: [nameAr, nameEn, price, imageColor, brand, model]

    ReservationStoreSnapshot:
      type: object
      description: Embedded store summary within a ReservationDto — avoids a second round-trip
      properties:
        nameAr: { type: string }
        nameEn: { type: string }
        logoColor: { type: string }
        logoInitial: { type: string }
        address: { type: string }
        phone: { type: string }
      required: [nameAr, nameEn, logoColor, logoInitial, address, phone]

    ReservationDto:
      type: object
      description: >
        Full reservation record with embedded product and store snapshots.
        Status values: pending | confirmed | declined | cancelled | expired | completed.
      properties:
        id: { type: string }
        productId: { type: string }
        product:
          $ref: "#/components/schemas/ReservationProductSnapshot"
        storeId: { type: string }
        store:
          $ref: "#/components/schemas/ReservationStoreSnapshot"
        buyerId: { type: string }
        conversationId:
          type: string
          nullable: true
        status: { type: string }
        cancelledBy:
          type: string
          nullable: true
          description: "Who cancelled: 'buyer' | 'merchant' | null"
        buyerNotes:
          type: string
          nullable: true
        merchantNotes:
          type: string
          nullable: true
        expiresAt: { type: string }
        confirmedAt:
          type: string
          nullable: true
        declinedAt:
          type: string
          nullable: true
        cancelledAt:
          type: string
          nullable: true
        completedAt:
          type: string
          nullable: true
        createdAt: { type: string }
        updatedAt: { type: string }
      required:
        - id
        - productId
        - product
        - storeId
        - store
        - buyerId
        - status
        - expiresAt
        - createdAt
        - updatedAt

    CreateReservationBody:
      type: object
      description: Request body for creating a reservation on a product.
      properties:
        buyerId:
          type: string
          description: Buyer's device UUID (anonymous identity until auth is added)
          minLength: 1
        buyerNotes:
          type: string
          description: Optional message from the buyer to the merchant (max 500 chars)
          maxLength: 500
      required: [buyerId]

    MerchantActionBody:
      type: object
      description: Request body for confirm and complete actions — requires the merchant's store ID.
      properties:
        storeId:
          type: string
          description: Store ID of the acting merchant
          minLength: 1
      required: [storeId]

    DeclineReservationBody:
      type: object
      description: Request body for merchant decline action.
      properties:
        storeId:
          type: string
          description: Store ID of the acting merchant
          minLength: 1
        cancellationReason:
          type: string
          description: Optional reason shown to the buyer (max 500 chars)
          maxLength: 500
      required: [storeId]

    CancelReservationBody:
      type: object
      description: >
        Request body for cancellation. Provide exactly one of buyerId (buyer cancels)
        or storeId (merchant cancels). The other party will be notified.
      properties:
        buyerId:
          type: string
          description: Buyer device UUID (when the buyer is cancelling)
        storeId:
          type: string
          description: Store ID (when the merchant is cancelling)
        cancellationReason:
          type: string
          description: Optional reason (max 500 chars)
          maxLength: 500

    ReservationConflictError:
      type: object
      description: Returned when a reservation cannot be created due to a conflict.
      properties:
        error: { type: string }
        code:
          type: string
          description: "DUPLICATE_RESERVATION (same buyer) or PRODUCT_ALREADY_RESERVED (another buyer)"
      required: [error, code]

    # ─── Auth schemas ─────────────────────────────────────────────────────────

    UserDto:
      type: object
      description: Public user profile returned from auth endpoints.
      properties:
        id: { type: string }
        email: { type: string }
        name: { type: string }
        nameAr: { type: string }
        role:
          type: string
          enum: [buyer, merchant, moderator, admin]
        storeId:
          type: string
          nullable: true
        isEmailVerified: { type: boolean }
        createdAt: { type: string }
      required: [id, email, name, nameAr, role, isEmailVerified, createdAt]

    AuthTokensResponse:
      type: object
      description: Issued on successful login or register.
      properties:
        accessToken: { type: string }
        refreshToken: { type: string }
        expiresIn:
          type: integer
          description: Access-token TTL in seconds
        user:
          $ref: "#/components/schemas/UserDto"
      required: [accessToken, refreshToken, expiresIn, user]

    RegisterBody:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
          minLength: 8
        name:
          type: string
          minLength: 2
        nameAr:
          type: string
          description: Arabic display name (falls back to `name` if omitted)
        role:
          type: string
          enum: [buyer, merchant]
          default: buyer
        storeId:
          type: string
          description: Required when role is merchant
        deviceId:
          type: string
          description: Device UUID for session tracking
        platform:
          type: string
          enum: [mobile, web]
      required: [email, password, name]

    LoginBody:
      type: object
      properties:
        email:
          type: string
        password: { type: string }
        deviceId:
          type: string
          description: Device UUID for session tracking
        platform:
          type: string
          enum: [mobile, web]
      required: [email, password]

    RefreshBody:
      type: object
      properties:
        refreshToken:
          type: string
          description: The long-lived refresh token issued at login
      required: [refreshToken]

    LogoutBody:
      type: object
      properties:
        refreshToken:
          type: string
          description: Optional — revokes this specific token as well as the session
      required: []

    ForgotPasswordBody:
      type: object
      properties:
        email:
          type: string
      required: [email]

    ResetPasswordBody:
      type: object
      properties:
        token:
          type: string
          description: One-time reset token from the forgot-password email
        newPassword:
          type: string
          minLength: 8
      required: [token, newPassword]

    VerifyEmailBody:
      type: object
      properties:
        token:
          type: string
          description: One-time verification token from the verification email
      required: [token]

    MessageResponse:
      type: object
      description: Generic success message
      properties:
        message: { type: string }
      required: [message]

    ErrorResponse:
      type: object
      properties:
        error: { type: string }
        code:
          type: string
          nullable: true
      required: [error]

  responses:
    BadRequest:
      description: Invalid request body or parameters
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Missing or invalid authentication
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Insufficient permissions
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
